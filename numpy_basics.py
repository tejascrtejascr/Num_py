import numpy as np

numbers = np.array([10, 20, 30, 40, 50])

print("Numbers:", numbers)
print("Total:", np.sum(numbers))
print("Average:", np.mean(numbers))
print("Largest:", np.max(numbers))
print("Smallest:", np.min(numbers))